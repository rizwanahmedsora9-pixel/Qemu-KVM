Place your cross-compiled `qemu-system-mips` binary in this directory
(app/src/main/assets/) named exactly:

    qemu-system-mips

It must be built for the target device ABI (arm64-v8a / armeabi-v7a /
x86_64) and statically linked, or ship its shared library dependencies
alongside it and adjust AssetInstaller to copy/chmod those too.

Delete this placeholder file once the real binary is in place.
