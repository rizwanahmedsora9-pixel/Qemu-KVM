# Project-specific R8/ProGuard rules.
# Keep the entry points R8 already discovers automatically; nothing extra
# is required for this Compose app with minify disabled. Kept so the
# release buildType's proguardFiles(...) reference resolves in CI.
